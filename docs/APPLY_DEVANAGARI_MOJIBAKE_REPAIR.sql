-- =============================================================================
-- APPLY_DEVANAGARI_MOJIBAKE_REPAIR.sql
--
-- Do NOT run this file in Supabase SQL Editor.
-- Canonical paste target (Meta-Supervisor 1 / A+B resolution):
--
--   docs/APPLY_UTF8_MOJIBAKE_REPAIR.sql
--
-- Why one script:
--   A (source+DB): corruption is stored WIN1252 mojibake — repair with
--     convert_from(convert_to(s, 'WIN1252'), 'UTF8') on à¤/à¥ rows.
--   B (import+presentation): fixUtf8 / fixMojibake now structurally repair
--     the same pattern at read/ingest — defense only, not a DB substitute.
--   APPLY_UTF8 covers Devanagari chapters + Western/math leftovers in one go.
-- =============================================================================
