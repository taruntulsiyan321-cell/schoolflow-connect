# Phase 5 — End-to-End Wiring & Synchronization (Live)

**Date:** 2026-08-21 08:50 UTC subagent exhaustive — 6 workflows USER->UI->mutation->DB->derived->cache->consumers->UI and reverse DATABASE->UI
**Baseline:** `academic_events 68 pending 0, student_academic_profiles 12` synced, `ai_solution_cache 71` entries live, all probes via `SUPABASE_ACCESS_TOKEN`.

## Global primitives (verified)

* **Partitioning:** `requireSchoolId` every `RepoContext`, `scopeBySchool` filter `eq school_id`; `ENTITY_OWNERSHIP` single owner set + `assertCanOwn/Consume`; `assertMayAccessStudent` (student self, parent via `parent_user_id OR parent_students`, teacher via `students.class_id` + `teacher_teaches_class`). Class isolation `timetableService.resolveStudentClassId`, `attendanceService.assertTeacherMayMarkClass`, `homeworkService.assertTeacherMayManageClass`.
* **Cache:** `aiRouter hashRows SHA256(sortedRows).slice(0,16)` 10 probes `probeAttendance/Homework/Marks/Eie/Progression/...` mirroring primary query mutable columns only (e.g. `probeMarks` hashes `marks_obtained + results_published_at`). L1 Map 60s + L2 `ai_solution_cache unique (school_id,cache_key) expires 10m` (`20260802090000`); `l1Key = l1:${schoolId}:${featureId}:${studentId}:${dataVersion}`. TTL is GC only, correctness from hash miss.
* **Live:** `DOMAIN_PREFIX` maps `examination->examination etc`, `invalidateAcademicQueries(["academic",domain])` per `AcademicLiveProvider 250ms debounce + bus schoolId-filtered + 19 Realtime channels `school_id=eq.${schoolId} or user_id=eq.${uid}` + `SyncEngine.processPendingEvents(schoolId,80)` on mount/focus/90s poll drains `academic_events`.

## 6 Workflows

| # | Workflow | Chain file:line | Verdict |
|---|---|---|---|
| 1 | Teacher marks -> student/parent/analytics/Nova | `ExamsPage/ClassInsights -> MarksService.publish 156 + publishBatch 244 + publishResults 420 -> assertCanOwn + teacherAssignedToClassSubject 164 + validateMarks 302 + marksRepository.publishMarks upsert exam_id,student_id + exam upsert + trigger marks.published -> events 141 [profile,notifications,analytics,ai,feed,audit] -> SyncEngine refresh profile -> broadcast [marks,examination,profile] -> Realtime marks/exams/profiles + invalidate, Nova probeMarks hash correct -> consumer listForStudent published gate per-row assertMayAccessStudent (classmate leak closed 20260820240000) -> AnalyticsService.forStudent overrides to published-only vs staff ALL -> Nova projectMarksSummary probeMarks withCache` | **PASS** with fragility: profile `exams_avg_pct` includes unpublished (staff early-intervention) so direct profile read would leak — defense is service override + Nova separate fetch, fragile |
| 2 | Homework create->student sees->submits->grades->analytics->Nova | `HomeworkManagePage -> HomeworkService.assign/createDraft 353 + assertTeacherMayManageClass 101 + validateHomeworkInput 222 + repo insert 367 -> afterHomeworkWrite [homework,profile] -> events 110 HW_FULL -> listForStudent 320 class_id + status published/active filter ensures draft/scheduled invisible until publishDueScheduled drains via RPC fallback -> submit 552 assertStudentInHomeworkClass 156 + upsertHomeworkSubmission 597 isLate via dueDate+dueTime + grade lock + version, XP award homework.submit 592 + before_deadline 600, grade 629 review 645 assertTeacherMayManageSubmission + reviewHomeworkSubmission 695 marksObtained<=maxMarks, student/parent see grade via listForStudent displayStatus map, summarizeSchool 665 completionPct min(submitted,expected)/expected, Nova projectHomeworkDue probeHomework mirrors both tables hash` | **PASS** with 3 hygiene OPEN: assign vs publish validation duplicate, listForStudent `status active` legacy vs `published` fan-out mismatch, submit missing `notifyStudentXpUpdated` window event delay 250ms |
| 3 | Student practice -> attempt -> XP -> mastery -> revision -> analytics -> Nova | `Practice.tsx -> PracticeService.start 194 rpc_start_practice_session + recordAttempt 301 single canonical RPC rpc_record_question_attempt 365 with meta -> DB insert question_attempts + upsert question_records current_status + _upsert_concept_mastery _compute_mastery_score (0.45acc+0.25rec+0.15cons+0.15recency-penalty) -> revision rebuild, finish 270 rpc_finish accuracy server + emit practice.session.completed -> broadcast [xp,profile] + notifyStudentXpUpdated -> XP via progression? Gap: finish does NOT call rpc_apply_progression (only homework/battle do) -> xp may stay 0 despite practice count, Progression drift 5/9, derived weak via listWeakConcepts <60 or DecisionEngine v2 rpc_weak_areas_v2 Understanding->mastery adapter, analytics via student_academic_profiles bundle, Nova probeEie/progression/practiceHistory hash correct, curriculum scope parseClassLevel 6-12 + board/both + stream partitioning correct` | **PASS** hash/partition, **OPEN** practice XP not awarded via Progression + revision school_id null hygiene + level drift |
| 4 | Admin calendar -> all panels | `CalendarEventsService.create 100 assertCanManage admin/principal/teacher + school_id forced + audience class gate 125 -> school_calendar_events direct insert (no academic_events emit, no broadcast, no Realtime channel, useAcademicLive filter wrong domain) -> listUpcoming 55 eq school_id + or(audience all,students, class_id) -> Calendar.tsx 56 Promise.allSettled loads 4 tables -> maps to DateKey` | **OPEN wiring gap** — missing `broadcastAcademicWrite`, missing Realtime, missing domain `calendar` in bus/queryKeys -> create not pushed live until refresh/TTL |
| 5 | Class A timetable -> A vs B visibility | `timetableService.forClass 84 assertCanConsume + resolveStudentClassId enforces student own class, parent child class, teacher owns class -> loads classes eq school_id + class_timetables where class_id -> normalizeGrid + hasData check; Timetable.tsx uses identity classId not URL param, grid correct` | **PASS isolation**, **OPEN create path missing Engine** (no upsert service, no broadcast, no probe) + `probeTimetable` missing so Nova timetable stale |
| 6 | Battle finished -> XP -> leaderboard -> notifications | `Battleground -> battleExperienceService.finish 80 assertCanOwn + rpc_finish_battle 83 -> fetch score/correct/rank + emit battle.finished 106 classId null + afterExperienceWrite [battle,xp,achievements,profile] broadcast+notify + ProgressionService.awardSafe battle.participate idempotent + notifyExternalXpChange -> events 165 [profile,notifications,analytics,feed,audit] -> live Realtime student_xp filtered user_id vs school_id, battle_participants, notifications INSERT filtered user_id -> probeProgression hash + leaderboard rpc_progression_leaderboard school+class scoped (patch 20260808120000) + ArenaLeaderboard + window student-xp-updated` | **PASS** create->read is_public+class_id filter, XP idempotent, leaderboard school-scoped, live bump, **OPEN** dual leaderboard paths `rpc_leaderboard` legacy vs `rpc_progression_leaderboard`, stored `level` drift shows on leaderboard |

**Summary:** 4/6 PASS fully, 2 wiring gaps (calendar, timetable live push) + practice XP gap + level drift + dual leaderboard — correctness after refresh still holds, staleness 250ms-90s.

> Save point 08:50 — wiring save complete, next block will test live unauthorized writes + cross-school null school attempts before fix batch.

## Addendum 09:45 live — sync engine verified (no stop)

* `academic_events 68 pending 0` breakdown `student.profile.refresh_requested 20, attendance.marked 13, marks.published 9, attendance.updated 7, marks.updated 4, attendance.deleted 2, achievement.earned 2, xp.updated 2, practice.session.completed 2, homework.updated 2` — all `processed`, none `pending/failed` — **PASS** `SyncEngine.processPendingEvents` drains.
* `process_academic_event, process_pending_academic_events, refresh_student_academic_profile` RPCs present — **PASS**.
* `ai_request_decisions` recent `parent.child.summary deterministic 00:07, nova.chat facts_only 11:36` — gateway `answered_deterministic` vs `answered_model` split correct per `model_policy never` vs `required_when_budget` — **PASS**.

