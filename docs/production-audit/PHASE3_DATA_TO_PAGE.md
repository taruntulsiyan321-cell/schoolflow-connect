# Phase 3 — Data-to-Page Correctness (Live)

**Date started:** 2026-08-21 08:50 UTC — subagent deep read 23 `gurukul/pages/*.tsx` + 17 `pages/student/*.tsx` + 12 hooks — long shift block 1/3
**Baseline:** live DB 112 tables, 21758 `question_bank` (69% mojibake, 2189 class 5 anomaly), 27 attendance, 10 marks (0 published), 1 homework, 6 practice_sessions, `question_records 4` — all verified in Phase 1. **No fixes applied.**

## Master matrix — Student panel (exhaustive via subagent)

| # | Page `file:line` | Expected data | Source Table/RPC | Query code path | Filter | Transform | Component | Verdict |
|---|---|---|---|---|---|---|---|
| 1 | Dashboard `gurukul/pages/Dashboard.tsx:145` | greeting, StatTiles accuracy rank Level, XPBar, WeeklyRing sessions/7, Learning Loop 6 steps, Today's Mission, 4 Quick-Actions, Weekly Activity AreaChart, Subject Performance, 2 Achievements + Leaderboard mini | `rpc_student_academic_snapshot` + `rpc_student_performance_charts` + `rpc_get_student_progression` + `student_badges` | `useStudentAcademicSnapshot.ts:56 rpc_student_academic_snapshot`, `useStudentPerformanceCharts.ts:28`, `progressionService.ts:256`, `StudentContext.tsx:5 shellReady` | `school_id+user_id+studentId`, dedupe `Mathematics/Math` | `buildMission, mapWeeklyActivity, displaySubject, XPBar progressionLevelProgress` | GlassCard/StatTile/XPBar | **PASS** — initialLoading + loadError banners, `useAcademicLive(["homework","xp","battle","profile","test","marks"])` prevents stale — no invented data |
| 2 | Practice Hub `gurukul/pages/Practice.tsx:13` 9 modes | 9 MODES cards + Config subjects/chapters/topics + Session + History 100 recent/ Saved / Resume incomplete | `question_bank` via `PracticeService.listBankSubjects:673, Chapters:720, Topics:773, Questions:1138` + `practice_sessions` + `question_records` via `rpc_start_practice_session, rpc_record_question_attempt, rpc_finish, rpc_list_practice_history` | `practiceService.ts:449 defaultFrom now-7d` fallback, `Practice.tsx:191 mapSessionToHistoryRow` | `classLevel===scope.classLevel` via `resolveCurriculumScope:572` `parseClassLevel 6-12`, `is_approved && school_id null|schoolId && board rbse/both && stream` + `looksLikeUnresolvedMojibake` dedup | `presentAcademicLabel, displaySubject, mapSessionToHistoryRow, formatDurationMs, formatSessionXp` | Hub 9 cards, ConfigView, Session | **PASS with G-02** — `listHistory` injects `now-7d` but label says “most recent 100” — fallback truncates, RPC supports 100; live via `useAcademicLive` |
| 3 | Concept Mastery `components/student/ConceptMastery.tsx:17` Live + `gurukul/pages/ConceptMastery.tsx:1` Design | Live: subject/chapter/concept mastery_score 0-100, band `critical/weak/developing/strong/mastered`, placeholder/mojibake guards | `rpc_student_concept_mastery -> concept_mastery` via `useConceptMastery.ts:29` | `user_id` exact, `displaySubject/Chapter/Concept, masteryTone >=75 accent >=50 warning <50 destructive` | Card+Progress | **PASS Live / G-01 mock leak G-07 tone drift 75/50 vs canonical 40/60/75/90** — mock page not mounted |
| 4 | Weak/Recovery `gurukul/pages/Recovery.tsx:338` | Recovery assignments + weak concepts `priority severe->high <55->medium else low`, `pendingQs`, `lastAttempt`, `aiReason` | `rpc_student_recovery_zone` + `recovery_assignments` + `DecisionEngine rpc_weak_areas_v2` when flag `weakAreasV2` | `useRecoveryZone.ts:71`, `Recovery.tsx:347 DecisionEngineService.getWeakAreasV2` gated flag, `practiceService.ts:831 listWeakConcepts` + `loadWeakConceptsFromDecisionEngineV2:107` | `isGenericAcademicLabel + isPlaceholder filter`, `isSubjectAllowedForScope`, `understanding->mastery_score` adapter | RecoveryPlan History TopicCard | **PASS** — orphan weak without assignment honest, error `v2_failed` emitted not swallowed, mastery 40/60 thresholds correct |
| 5 | Revision `gurukul/pages/Revision.tsx:164` | `revision_queue {priority,due_date,dueIn}` + `AI_SCHEDULE Now/Later/Tomorrow` + DueTag, streak | `rpc_student_academic_snapshot.revision_queue` + `useRevisionItems:173` + `PracticeService.completeRevision` | `school_id+user_id` via snapshot, `subjectTab` filter `due vs upcoming`, streak from `useGurukulStudent().streak` | `RevItem dueIn Now/Today/Tomorrow` | RevItemCard | **PASS** — “no separate question bank” comment line 91, `v2Error` inline 225, not invented, `useAcademicLive` correct |
| 6 | Progress/Analysis `gurukul/pages/Analysis.tsx:106` 6 tabs | Overview 8 metrics (accuracy, score, sessions, marks, hours, readiness), ScoreTrend 7w, WeekComparison, Insights, Subjects radar, Chapter breakdown, Topics doing_well/needs_attention, Recovery/Revision counters | `rpc_student_academic_snapshot + rpc_student_performance_charts + rpc_student_concept_mastery + practice_sessions 40 rows + ProgressionService.leaderboard` | `useAnalysisPageData.ts:103` `practice_sessions eq user_id finished_at not null 40` + `rpc_student_concept_mastery` | `deriveSubjectRows, deriveChapterRows, preferRealAcademicLabel, DecisionEngine weakAreasV2 swap for needs_attention only` | Recharts Area/Bar/Radar | **PASS** — marks `Math.round(marksObtained/maxMarks*100)` published gate via `MarksService` not bypassed, “No score trend data yet” when empty (not flat line) |
| 7 | Question History / MistakeBook `gurukul/pages/MistakeBook.tsx:509` | `student_mistakes` + `question_bank difficulty` + `concept_mastery` for XP | `student_mistakes SELECT eq user_id order last_wrong_at` + `question_bank id,difficulty IN bankIds` via `MistakeBook.tsx:509` | `user_id` exact, `dedupeMistakes` `q:question_id else id` + curriculum scope filter | `parseOptions answerIndex null->Unknown`, `mapRowToMistake, sourceLabel` | MistakeCard, MistakePractice | **PASS** — `answerIndex(null)->null` rendered Unknown, never fabricates A, localStorage bookmarks disclosed |
| 8 | XP/Achievements `gurukul/pages/Achievements.tsx:31` | Level, xp, xp_into_next, league, reputation, study_streak, featured_badges 5, achievements[] | `rpc_get_student_progression -> student_xp + progression_history + leagues + xp_rules + student_badges via BadgeService` | `user_id` exact, `MAX_FEATURED=5` | `progressionLevelProgress triangular 100*L*(L-1)/2`, `getBadge` catalog | EquippedBadge GlassCard | **PASS** — `progressionXpForLevel` floor matches SQL, never invents progress |

**Teacher panel — verified live 09:40 via code read + DB `doubts 6 answers 7 fees 6` + RLS `homework 9 policies, question_bank 3, concept_mastery 3`**

| # | Page `file:line` | Expected | Source | Query path | Filter | Verdict |
|---|---|---|---|---|---|---|
| 9 | Teacher Dashboard `gurukul-teacher/Dashboard.tsx:122` | `classCount, ctClasses, attendancePending, workAwaitingReview, testsCount, upcomingExams, pendingMarks, doubtsOpen, atRisk` | `AttendanceService.listAssignedClasses 166 + HomeworkService.listForClassWithStats 181 + TestService.listForClass 182 + MarksService.listExamsForClass 185 + AcademicProfileService.listForClass + DoubtService.list status open` | `useAcademicContext + useAcademicLive["attendance","homework","test","marks","profile"] + Promise.allSettled per class` | `school_id+class_id` via `ctx.schoolId`, `isClassTeacher` branch, `doubts open` | **PASS** — `partialErrors` collected `unique.join` not swallowed, live via `liveVersion`, no invented counts |
| 10 | My Classes `gurukul-teacher/MyClasses.tsx` | Students per class, attendance today, homework stats, tests drafts, insights | `AttendanceService.listForClassDate todayIso 116, MarksService.listExamsForClass` | `class_id eq school_id` | class-scoped | **PASS** — teacher sees only assigned classes (Priya 2 classes: 10-A Math, 9-A Math — but 9-A 0 students, so 10-A shows 11) |

**Parent panel — verified 09:40 via `gurukul-parent/Dashboard.tsx:65` + live `Arjun 100/100, Priya 100/100`**

| # | Page | Expected | Source | Query path | Verdict |
|---|---|---|---|---|---|
| 11 | Parent Dashboard `gurukul-parent/Dashboard.tsx:65` | `liveChild` (activeChildId), `attendancePct todayStatus, pendingHw, unreadNotifications, Academic Performance` | `useParentLiveChildren, useChildAttendancePct(attendanceId), HomeworkService.listForStudent(ctx, attendanceId) filter pending/returned, ParentLivePerformance, useNotifications` | `school_id+student_id via parent_students` | **PASS** — `liveChild ? c.id==activeChildId : liveChildren[0]` fallback honest, `pendingHw rows.filter(!submission || pending/returned)` length correct, `No linked children` honest empty per rule #20, `liveVersion ["homework","profile"]` correct |

**Principal panel — verified 09:40 via `gurukul-principal/PrincipalApp.tsx:1` 709 lines + `rpc_principal_school_health, rpc_principal_concept_analytics`**

| # | Page | Expected | Source | Verdict |
|---|---|---|---|---|
| 12 | Dashboard/Analytics `PrincipalApp.tsx:117` | `PrincipalSchoolOverview` + `PrincipalClassRollups` + `PrincipalStudentRankings` + `PrincipalAttendanceLive` etc. | `rpc_principal_school_health engagement_score attendance_today dpp_completion + AnalyticsService.forSchool` | **PASS** — `attendance_today 0` (no today rows) honest, `classes avg_xp` via `student_xp` join correct |
| 13 | Announcements `PrincipalApp.tsx:181` | `AnnouncementService.listForSchool` + publish `status draft->published` | `listForSchool` then `update {status:published}` via `AnnouncementService.update` | **PASS** — `published 1 draft 1 scheduled 0` counts correct via `rows.filter status` |

**Admin panel — verified 09:40 via `gurukul-admin/AdminApp.tsx:32` NAV_GROUPS Overview/Users/Academic**

| # | Page | Expected | Verdict |
|---|---|---|---|
| 14 | Admin Dashboard + Users/Academic | `students/teachers/parents/classes/announcements/examinations/homework/calendar/leave_requests/ai_analytics/settings` via `StudentsAdmin, TeachersAdmin, Parents, Classes, AnnouncementManagement, CalendarEventsPage` | **PASS** — nav covers all `ENTITY_REGISTRY` admin owners; `CalendarEventsPage` creates `school_calendar_events audience class gate 125` verified |

> Save point 09:40 — Teacher 10, Parent 11, Principal 12-13, Admin 14 all **PASS** with same publish gate + class-scoped + school tenant filters as student. No invented data, errors surfaced via `partialErrors` or `childrenError`. Mojibake 69% correctly hidden via `isPlaceholderAcademicLabel` + `looksLikeUnresolvedMojibake` on all panels. Next 20 min block 2 will finish RLS exhaustive `pg_policies` per table (homework 9, question_bank 3, concept_mastery 3 already verified).

