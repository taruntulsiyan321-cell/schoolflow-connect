Deploy ALL Supabase Edge Functions for SchoolFlow Connect from the connected GitHub repo (main branch, latest commit).

Project: kdmjipeksjdyojjdokbi (Lovable Cloud Supabase)

## Goal
ai-explain and other AI functions currently return 404. Deploy every function under supabase/functions/ so the student app can use Google Gemini Flash AI.

## Secret (already set — verify only)
Confirm Edge Function secret exists:
- GOOGLE_GEMINI_API_KEY

Optional fallback: LOVABLE_API_KEY

## Functions to deploy (all of them)
1. ai-explain          (uses ./gemini.ts — Explain my mistake)
2. ai-battle-report    (uses ./gemini.ts — battle coach)
3. ai-improvement-plan (uses ./gemini.ts — improvement plans)
4. ai-concept-report   (uses ./gemini.ts — concept recovery)
5. dpp-generate-questions (uses ./gemini.ts — teacher DPP gen)
6. send-otp            (already live — redeploy if needed)
7. verify-otp
8. send-push
9. admin-link-account

Each AI function is self-contained: index.ts imports from ./gemini.ts in the SAME folder (NOT ../_shared/).

## Deploy command
Use Supabase CLI against project kdmjipeksjdyojjdokbi:

supabase functions deploy ai-explain ai-battle-report ai-improvement-plan ai-concept-report dpp-generate-questions send-otp verify-otp send-push admin-link-account --project-ref kdmjipeksjdyojjdokbi

Or deploy all functions in supabase/functions/ in one go if supported.

## verify_jwt (from supabase/config.toml)
- send-otp, verify-otp: verify_jwt = false
- All AI functions + dpp-generate-questions + admin-link-account: verify_jwt = true

## After deploy — verify
1. OPTIONS https://kdmjipeksjdyojjdokbi.supabase.co/functions/v1/ai-explain → must NOT be 404
2. OPTIONS https://kdmjipeksjdyojjdokbi.supabase.co/functions/v1/ai-battle-report → must NOT be 404
3. In the app: wrong answer → "Explain my mistake" → should show Gemini Flash badge (not Offline coach)

## Do NOT
- Do not change frontend code unless deploy fails
- Do not use SQL Editor for this (edge functions are not SQL)
- Do not split into batches — deploy all functions in one session

Report back: which functions deployed, and whether ai-explain OPTIONS returns 200.
