Apply ALL pending database updates for SchoolFlow Connect in ONE go.

Use the Lovable project's Supabase database (kdmjipeksjdyojjdokbi). Do NOT split into batches.

Steps:
1. Open the file: supabase/LOVABLE_PASTE_ALL_PENDING.sql
2. Copy the ENTIRE file (Ctrl+A, Ctrl+C)
3. In Lovable: Project → Supabase → SQL Editor → New query
4. Paste and click RUN once

That single file includes:
- All 16 pending migrations (student success, battleground, concept mastery, etc.)
- Demo school data (demo login accounts)
- 1373 Class 12 Mathematics practice templates

Do NOT use AI credits. Run the SQL directly.

After it finishes, confirm:
- Demo login works: arjun.mehta@wisdomcampus.demo / DemoPass123!
- SELECT count(*) FROM question_templates; returns about 1373

If the paste is too large for the editor, tell me and I will split only the math templates into a second paste — everything else stays in one file.
