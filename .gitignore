# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
supabase/.temp

# Playwright — never commit a captured login session or its report/trace output.
/e2e/.auth/
/playwright-report/
/test-results/
/blob-report/

# Generated question-bank content cache (intermediate; compiled migration is the real artifact)
scripts/rbse-commerce-full/.gen-cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Data exports taken before a destructive migration (Chunk 3.5 dropped
# library_books, library_checkouts, staff_attendance). These hold real staff
# attendance dates and are kept OUTSIDE the repo — git history is forever.
docs/data-exports/

# tsc -b writes these beside each tsconfig; `npm run typecheck` is build mode
# since 10.7 group 1, so they appear on every typecheck run. Build output.
*.tsbuildinfo
