/**
 * @deprecated Import from `@/lib/utf8MojibakeRepair` (or `@/lib/utf8Text` re-exports).
 * Kept as a thin alias so parallel supervisors do not diverge on APIs.
 */
export {
  looksLikeUtf8Mojibake,
  repairUtf8Mojibake,
  UTF8_MOJIBAKE_SIGNATURE,
} from "@/lib/utf8MojibakeRepair";
